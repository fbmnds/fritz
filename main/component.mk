#
# Main Makefile. This is basically the same as a component makefile.
#

COMPONENT_EMBED_TXTFILES := secrets/server.pem
COMPONENT_EMBED_TXTFILES += secrets/server-key.pem
COMPONENT_EMBED_TXTFILES += ipify/ipifyorg.pem
COMPONENT_EMBED_TXTFILES += telegram/telegram.pem
